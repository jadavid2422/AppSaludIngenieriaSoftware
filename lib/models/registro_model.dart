import 'package:flutter/cupertino.dart';

class RegistroModel extends StatelessWidget {
  const RegistroModel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }

}